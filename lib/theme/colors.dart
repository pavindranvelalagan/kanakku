import 'package:flutter/material.dart';

class AppColors {
  // Soft, premium palette
  
  // Primary: A soft yet vibrant blue, good for trust and finance
  static const Color primary = Color(0xFF4E71F3); 
  
  // Secondary: A warm pastel accent
  static const Color accent = Color(0xFFFFD464);
  
  // Backgrounds
  static const Color backgroundLight = Color(0xFFF8F9FE);
  static const Color backgroundDark = Color(0xFF121212);
  
  // Surfaces (Cards, etc.)
  static const Color surfaceLight = Colors.white;
  static const Color surfaceDark = Color(0xFF1E1E1E);
  
  // Text
  static const Color textPrimaryLight = Color(0xFF1A1C24);
  static const Color textSecondaryLight = Color(0xFF8E92A3);
  
  static const Color textPrimaryDark = Color(0xFFFFFFFF);
  static const Color textSecondaryDark = Color(0xFFAAAAAA);

  // Functional
  static const Color success = Color(0xFF4CD964);
  static const Color error = Color(0xFFFF3B30);
  static const Color warning = Color(0xFFFFCC00);
}
